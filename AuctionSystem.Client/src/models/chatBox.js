export default class User {
    constructor(username, recipient, visible, collapsed) {
        this.username = username
        this.recipient = recipient
        this.visible = visible
        this.collapsed = collapsed
    }
}