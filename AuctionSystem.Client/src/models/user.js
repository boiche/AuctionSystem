export default class User {
    constructor (username, fullName, email, phone) {
        this.username = username
        this.fullName = fullName
        this.email = email
        this.phone = phone
    }
}